# Optional AI conversation. Never use NEXT_PUBLIC_ for secrets.
OPENAI_API_KEY=
OPENAI_MODEL=
CORE_AI_ENABLED=false
# Set only after configuring persistent request limits for POST /api/core in your deployment gateway.
CORE_RATE_LIMIT_CONFIGURED=false
